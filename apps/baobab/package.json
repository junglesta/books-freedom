{
  "name": "@bookbat/baobab",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf .astro node_modules/.astro node_modules/.vite dist",
    "dev": "pnpm clean && astro dev --open",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@bookbat/library-core": "workspace:*",
    "astro": "^6.3.5"
  }
}
